spring.application.name = todo-app

spring.datasource.url = ${DB_URL:jdbc:mysql://localhost:3306/todo_test}
spring.datasource.username = ${DB_USERNAME:todo_test_user}
spring.datasource.password = ${DB_PASSWORD:testpassword123}

server.port = ${PORT:8080}

jwt.secret = ${JWT_SECRET:test-jwt-secret-key-with-32-char}

cookie.secure = ${COOKIE_SECURE:false}

cors.allowed-origin = ${CORS_ALLOWED_ORIGIN:http://localhost:3000}
